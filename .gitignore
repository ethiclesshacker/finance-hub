# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env files
.env
.env*.local

# Stray key material — belongs in ~/.ssh, never in a repo
*_ssh_key*
*.pem

# Personal Event Ledger — local job secrets. Never commit these.
.env.ledger
ledger/accounts.json

# Local cost ledger — operational data about this machine, not project state.
ledger/costs.jsonl
